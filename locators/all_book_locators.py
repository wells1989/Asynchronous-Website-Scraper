class BooksPageLocators:
    BOOKS = 'div.col-sm-8 section li.col-xs-6'

    PAGER = 'div.page_inner section ul.pager li.current'
    """
    NOTE: Could alt give fuller paths, but as long as it's identifiable it works
    e.g. alt path would be div.page_inner div.col-sm-8 ... etc
    """